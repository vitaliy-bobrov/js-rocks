import { PedalBoardDirective } from './pedalboard.directive';

describe('PedalBoardDirective', () => {
  it('should create an instance', () => {
    const directive = new PedalBoardDirective();
    expect(directive).toBeTruthy();
  });
});
